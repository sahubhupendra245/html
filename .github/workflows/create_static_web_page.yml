name: create static web
on: wrokflow_dispatch
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: Print greetings
        run: echo "Hello World"
      - name: Print files
        run: pwd
